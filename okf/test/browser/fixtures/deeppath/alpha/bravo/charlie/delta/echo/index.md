# Echo

The authored map for this deep directory. Because it is present, the Files
view's Indexes-only list renders it as a row carrying its whole path —
`alpha/bravo/charlie/delta/echo/index.md` — which is what must ellipsize rather
than push its badge off the edge.

* [Deep Service](service.md) - the one concept down here
