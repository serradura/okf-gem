{
  "name": "okf-browser-tests",
  "private": true,
  "type": "module",
  "description": "Playwright suite for lib/okf/render/graph/template.html.erb. Not part of the gem; not run by `rake`.",
  "scripts": {
    "test": "playwright test",
    "ui": "playwright test --ui",
    "headed": "playwright test --headed",
    "report": "playwright show-report",
    "setup": "npm install && playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
