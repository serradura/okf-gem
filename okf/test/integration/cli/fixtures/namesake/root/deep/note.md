---
type: Note
title: Note
description: A concept below the directory named root, so the prefix rule has something to reach.
tags: [process]
---

# Note

Below `root/`, which is what makes its subtree bigger than its direct count.
