---
okf_version: 0.2
---

# Aliased Id

One concept whose frontmatter `id` moves it out of its directory — the fixture
for the recorded rule: the id-derived views (catalog, hubs, --dir, search)
follow the id, the physical views (index, dirs, stats) follow the disk.

* [Orders](tables/orders.md) - files by path, addressed as `orders`
