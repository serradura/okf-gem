# Docs

The intermediate directory: it holds no concepts of its own, so it exists only
to connect the tree — which is exactly the row an ancestor chain must print.

* [Guides](Guides/) - the concepts themselves.
