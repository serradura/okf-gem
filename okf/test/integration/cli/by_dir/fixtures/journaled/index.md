---
okf_version: "0.2"
---

# Journaled

A bundle whose `root/` directory carries a `log.md` and nothing else — the
third file kind that makes a directory real. `hollow` proved the point for an
`index.md`; a scoped log is just as conformant (`okf log` reads every one),
and a directory holding only its history must still beat the `root` alias.

* [Charter](charter.md) - the only concept here
