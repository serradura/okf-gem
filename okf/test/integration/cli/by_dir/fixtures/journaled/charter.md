---
type: Decision
title: Charter
description: The one concept in this bundle, and it lives at the bundle root.
tags: [governance]
---

# Charter

`--dir .` selects this. `--dir root` must not, because a directory named
`root` exists and holds only a log.
