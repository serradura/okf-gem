# History

A directory whose only file is its own log — real to `okf dirs`, and the shape
okf 1.13.0 fixed the `root` alias against.

## 2026-08-03
* **Initialization**: Started keeping history here.
