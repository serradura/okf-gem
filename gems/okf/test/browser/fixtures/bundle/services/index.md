# Services

The running processes. This folder carries an authored map, so the page's
index layer draws it as a written map rather than a synthesized one — the
`datasets/` folder next door has no `index.md` and covers the other case.

* [Gateway](gateway.md) - the public edge
* [Billing](billing.md) - invoices and payment capture
